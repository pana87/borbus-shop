{
  "scripts": {
    "dev": "netlify dev",
    "start": "npm run clean && eleventy --serve",
    "build": "npm run clean && eleventy",
    "clean": "rm -rf ./public"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.0",
    "@hashgraph/sdk": "^2.12.1",
    "dotenv": "^16.0.0",
    "hashconnect": "^0.1.5",
    "nft.storage": "^6.2.0",
    "nodemailer": "^6.7.5",
    "web3.storage": "^4.0.0"
  }
}
