{
  "scripts": {
    "dev": "netlify dev",
    "start": "npm run build && node ./src/app.mjs",
    "build": "npm run clean && eleventy",
    "clean": "rm -rf ./public"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.0",
    "@paypal/paypal-js": "^5.1.1",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "nft.storage": "^6.2.0",
    "nodemailer": "^6.7.5"
  }
}
